batcmd: batsim -p ${BAD_DIR}/platforms/small_platform.xml -w ${BAD_DIR}/workload_profiles/test_workload_profile.json -e /tmp/robin/batsim_nosched_badbash/out --batexec
output-dir: /tmp/robin/batsim_nosched_badbash
schedcmd:
simulation-timeout: 5
ready-timeout: 5
success-timeout: 5
failure-timeout: 5
