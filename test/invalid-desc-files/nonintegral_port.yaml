batcmd: batsim -p ${BATSIM_DIR}/platforms/small_platform.xml -w ${BATSIM_DIR}/workload_profiles/test_workload_profile.json -e /tmp/robin/nonintegral_port/out -s tcp://localhost:noninteger
output-dir: /tmp/robin/nonintegral_port
schedcmd: batsched
simulation-timeout: 30
ready-timeout: 5
success-timeout: 5
failure-timeout: 0
