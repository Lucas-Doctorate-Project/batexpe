batcmd: batsim -p ${BATSIM_DIR}/platforms/small_platform.xml -w ${BATSIM_DIR}/workload_profiles/test_workload_profile.json -e unwritable-dir
output-dir: unwritable-dir
schedcmd: batsched
simulation-timeout: 30
ready-timeout: 5
success-timeout: 5
failure-timeout: 0
