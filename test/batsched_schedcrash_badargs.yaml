batcmd: batsim -p ${BATSIM_DIR}/platforms/small_platform.xml -w ${BATSIM_DIR}/workload_profiles/test_workload_profile.json -e /tmp/robin/batsched_schedcrash_badargs/out
output-dir: /tmp/robin/batsched_schedcrash_badargs
schedcmd: batsched --this-parameter-should-not-exist
simulation-timeout: 15
ready-timeout: 5
success-timeout: 1
failure-timeout: 0
